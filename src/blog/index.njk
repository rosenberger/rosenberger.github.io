---
layout: layouts/base.njk
title: Blog
---

<header class="mb-8">
  <h1 class="text-3xl font-semibold tracking-tight">Blog</h1>
  <p class="mt-2 text-slate-600 dark:text-white/80">Krátke poznámky a experimenty.</p>
</header>

<ul class="grid gap-4">
  {% set posts = collections.blogPosts | reverse %} {% for post in posts %}
  <li
    class="rounded-sm border border-slate-200/70 bg-white p-6 shadow-sm dark:border-white/20 dark:bg-brand"
  >
    <a
      class="text-lg font-semibold text-slate-900 hover:text-slate-700 dark:text-slate-900 dark:hover:text-slate-700"
      href="{{ post.url }}"
    >
      {{ post.data.title }}
    </a>
    <p class="mt-1 text-sm text-slate-500 dark:text-slate-700">
      <time datetime="{{ post.date | dateISO }}">{{ post.date | dateSK }}</time>
    </p>
    {% if post.data.excerpt %}
    <p class="mt-3 text-slate-700 dark:text-slate-800">{{ post.data.excerpt }}</p>
    {% endif %}
  </li>
  {% endfor %}
</ul>
