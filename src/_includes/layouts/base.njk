<!doctype html>
<html lang="sk">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{% if title %}{{ title }} · {% endif %}{{ site.title }}</title>
    <meta name="description" content="{{ description or site.description }}" />
    <link rel="stylesheet" href="/assets/css/style.css" />
    <script src="/assets/js/bundle.js" defer></script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Boldonse&family=Prompt:wght@400;600&display=swap"
      rel="stylesheet"
    />
  </head>
  <body class="min-h-dvh flex flex-col bg-brand text-slate-900 dark:bg-slate-600 dark:text-white">
    {% include "partials/header.njk" %}

    <main class="mx-auto w-full max-w-5xl flex flex-col flex-1 px-4 pt-24 pb-16">
      {{ content | safe }}
    </main>

    {% include "partials/footer.njk" %}
  </body>
</html>
